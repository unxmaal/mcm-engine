apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcm-engine.fullname" . }}
  labels:
    {{- include "mcm-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcm-engine.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: engine
