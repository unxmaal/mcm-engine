"""Adapter packages for mcm-engine.

Each subpackage implements one or more of the four Protocol contracts
defined in mcm_engine.backends. First-party adapters ship as optional
extras (pip install mcm-engine[postgres]); third-party adapters are
sibling distributions discovered via Python entry points.
"""
